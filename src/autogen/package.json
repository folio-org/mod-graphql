{
  "name": "raml2graphql",
  "version": "0.1.0",
  "description": "Translate RAML/JSON Schema into GraphQL schema",
  "repository": "folio-org/mod-graphql",
  "scripts": {
    "lint": "cd ../.. && yarn lint",
    "test": "node test/run-tests.js test",
    "regen": "node test/run-tests.js --regenerate test"
  },
  "publishConfig": {
    "registry": "https://repository.folio.org/repository/npm-folio/"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "json-schema-ref-parser-sync": "^1.0.0",
    "node-getopt": "^0.3.2",
    "raml-1-parser": "^1.1.42"
  }
}
