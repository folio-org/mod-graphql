# title: Schema-name with a hyphen in it
# raml-version: 0.9

type Query {
  person(name: String, maxCount: Float, queryString: String): Tperson_schema
}

type Tperson_schema {
  address: String
  name: String!
}

