# title: API that contains a link-field

type Query {
  person(name: String, maxCount: Float, queryString: String): TpersonSchema
}

type TpersonSchema {
  address: String
  aliases: [TpersonSchema_aliases] # link: /aliases (personId=$id) -> records
  name: String!
}

type TpersonSchema_aliases {
  alias: String
  inUseSinceYear: Int
}

