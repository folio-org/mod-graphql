# title: Extracted JSON schema, but still within this file

type Query {
  person(name: String, maxCount: Float, queryString: String): TpersonType
}

type TpersonType {
  address: String
  name: String!
}

