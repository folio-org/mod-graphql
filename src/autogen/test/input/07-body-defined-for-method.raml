#%RAML 0.8

title: simple API  

schemas:
  - personSchema: !include schemas/foo.json

/person:
  get:
    queryParameters:
      name:
      maxCount: {type: number}
      queryString: {type: string}
    body:
      application/json:
        schema: personSchema
