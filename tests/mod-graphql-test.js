import chai  from 'chai';
import mocha from 'mocha';

const { expect } = chai;
const { describe, it } = mocha;

describe('mod-graphql', function() {
  it('lives', function() {
    expect(1).to.equal(1);
  });
});
